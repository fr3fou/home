kind: HelmRepository
metadata:
  name: ingress-nginx
spec:
  url: https://kubernetes.github.io/ingress-nginx/charts
---
kind: HelmRelease
metadata:
  name: ingress-nginx
spec:
  chart:
    spec:
      chart: ingress-nginx
      version: "4.2.1"
      sourceRef:
        kind: HelmRepository
        name: ingress-nginx
  values:
    config:
      use-forwarded-headers: "true"
      enable-real-ip: "true"
