---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: steam-gateway
  namespace: apps
spec:
  parentRefs:
    - name: internal
      namespace: networking-system
  hostnames:
    - steam.nerv.id
  rules:
    - backendRefs:
        - name: steam-gateway
          port: 3001
