---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: luncher-box-frontend
  namespace: apps
spec:
  parentRefs:
    - name: external
      namespace: networking-system
  hostnames:
    - luncherbox.nerv.id
  rules:
    - backendRefs:
        - name: luncher-box-frontend
          port: 3000
