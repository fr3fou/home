---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: plex
  namespace: apps
spec:
  parentRefs:
    - name: external
      namespace: networking-system
  hostnames:
    - plex.nerv.id
  rules:
    - backendRefs:
        - name: plex
          port: 32400
