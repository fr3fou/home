---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hubble-ui
  namespace: kube-system
spec:
  parentRefs:
    - name: internal
      namespace: networking-system
  hostnames:
    - hubble.nerv.id
  rules:
    - backendRefs:
        - name: hubble-ui
          port: 80
